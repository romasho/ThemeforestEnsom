import styled from 'styled-components';

export const Video = styled.video`
  position: absolute;
  z-index: 20;
  max-width: 1110px;
  width: calc(100% - 50%);
  min-width: 328px;
  height: auto;
  margin: auto;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
`;
